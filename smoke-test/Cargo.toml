[package]
name = "storage-smoke-test"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "storage-smoke-test"
path = "src/main.rs"

[dependencies]
outlayer = { path = "../vendor/outlayer" }
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
strip = true
